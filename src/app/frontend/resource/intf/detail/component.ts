
// GENERATED BY THE UNION OF WAVESURF ROBOTS

/*
Copyright (c) 2019 WaveSurf Systems Inc. All rights reserved.
 */

import {Component, OnDestroy, OnInit} from '@angular/core';
import {ActivatedRoute} from '@angular/router';
import { IntfDetail} from '@api/gen.v1';
import {Subscription} from 'rxjs/Subscription';

import {ActionbarService, ResourceMeta} from '../../../common/services/global/actionbar';
import {NotificationsService} from '../../../common/services/global/notifications';
import {EndpointManager} from '../../../common/services/resource/endpoint';
import {ResourceService} from '../../../common/services/resource/resource';
import {Resource} from '../../../common/services/resource/endpoint.gen';

@Component({
  selector: 'kd-intf-detail',
  templateUrl: './template.html',
})
export class IntfDetailComponent implements OnInit, OnDestroy {
  private intfSubscription_: Subscription;
  private readonly endpoint_ = EndpointManager.resource(Resource.intf);
  intf: IntfDetail;
  isInitialized = false;
  eventListEndpoint: string;

  constructor(
    private readonly intf_: ResourceService<IntfDetail>,
    private readonly actionbar_: ActionbarService,
    private readonly activatedRoute_: ActivatedRoute,
    private readonly notifications_: NotificationsService,
  ) {}

  ngOnInit(): void {
    const resourceName = this.activatedRoute_.snapshot.params.resourceName;

    this.eventListEndpoint = this.endpoint_.child(resourceName, Resource.event);

    this.intfSubscription_ = this.intf_
      .get(this.endpoint_.detail(), resourceName)
      .subscribe((d: IntfDetail) => {
        this.intf = d;
        this.notifications_.pushErrors(d.errors);
        this.actionbar_.onInit.emit(new ResourceMeta('Interface', d.objectMeta, d.typeMeta));
        this.isInitialized = true;
      });
  }

  ngOnDestroy(): void {
    this.intfSubscription_.unsubscribe();
    this.actionbar_.onDetailsLeave.emit();
  }
}
